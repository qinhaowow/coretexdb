# CoreTexDB 功能模块文档

> 版本：v1.2.0 | 总模块数：46（全部编译通过）| 最后更新：2026-07-09

---

## 项目概述

CoreTexDB 是一个多模态向量数据库，面向 AI 应用提供向量检索、全文搜索、空间索引、分布式事务、混合检索等能力。采用模块化架构，每个 `coretex_*` 模块独立负责一个功能域。

### 技术栈

| 层级 | 技术选型 |
|------|---------|
| 异步运行时 | Tokio 1.x |
| 向量计算 | ndarray + Rayon 并行 |
| 存储引擎 | 内存 / RocksDB（可选） |
| 序列化 | Serde JSON + Bincode |
| Web 框架 | Axum 0.7 + Tower |
| gRPC | Tonic + Prost |
| GraphQL | async-graphql 7.0 |
| TLS | rustls 0.21 + rustls-pemfile |
| 加密 | AES-256-GCM + Argon2 + HMAC-SHA256 |
| ONNX 推理 | ort（可选） |
| Python 绑定 | PyO3（可选） |

### 特性开关 (Features)

| Feature | 描述 |
|---------|------|
| `default` | tokio, serde, compression, metrics |
| `full` | 全部功能（含 rocksdb, onnx, tls-gen） |
| `rocksdb` | 持久化存储引擎 |
| `onnx` | ONNX 模型推理 |
| `tls-gen` | 真实自签名证书生成（rcgen） |
| `python` | Python 绑定（PyO3） |

---

## 一、核心基础设施（6个模块）

### 1. coretex_core — 核心类型与错误

**文件**：`src/coretex_core/mod.rs`

**职责**：定义数据库的基础类型系统。

```
核心导出：
├── Vector              — 向量类型（Vec<f32> + 元数据）
├── Document            — 文档类型
├── CollectionSchema    — 集合模式（名称、维度、距离度量）
├── IndexConfig         — 索引配置
├── IndexType           — 索引类型枚举
├── DistanceMetric      — 距离度量（Cosine/Euclidean/DotProduct）
├── CoreTexError        — 统一错误类型
└── Result<T>           — 类型别名
```

### 2. coretex_storage — 存储引擎

**文件**：`src/coretex_storage/mod.rs`

**职责**：提供可插拔的存储后端。

```
├── StorageEngine (trait)     — 统一存储接口
├── MemoryStorage             — 内存实现（默认）
└── PersistentStorage         — RocksDB 实现（需 rocksdb feature）
```

### 3. coretex_index — 向量索引

**文件**：`src/coretex_index/mod.rs`

**职责**：向量相似性搜索索引。

```
索引类型：
├── BruteForceIndex  — 暴力搜索（精确）
├── HNSWIndex        — 分层可导航小世界图（近似、高性能）
├── IVFIndex         — 倒排文件索引（近似）
└── ScalarIndex      — 标量字段索引
```

### 4. coretex_data — 统一数据管理

**文件**：`src/coretex_data/mod.rs`（1524行）

**职责**：数据库的核心数据层，整合存储、索引、事务、Lakehouse。

```
DataManager:
├── 集合 CRUD（create/delete/rename/list）
├── 向量 CRUD（insert/get/delete/update/upsert）
├── 批量操作（bulk_insert/update/delete/upsert）
├── 搜索（search + filter）
├── 事务管理集成
├── Lakehouse 冷热分层
└── UnifiedStorageAdapter  — 统一存储适配器
```

### 5. coretex_query — 查询处理

**文件**：`src/coretex_query/mod.rs`

**职责**：查询规划与执行。

```
├── QueryPlanner           — 查询规划器
├── DefaultQueryProcessor  — 默认查询处理器
├── cost_model/            — 代价模型（IndexSelector, JoinPushdown）
│   ├── CostInput / CostEstimate
│   ├── JoinPlan / JoinType
│   └── OptimizationStats
└── QueryType / QueryParams / QueryResult
```

### 6. coretex_sql — SQL 执行引擎

**文件**：`src/coretex_sql/mod.rs`

**职责**：类 SQL 语法解析和执行。

```
├── SQLLexer               — 词法分析器
├── SQLParser              — 语法分析器（递归下降）
├── SQLExecutor            — 执行器
├── SQLStatement 枚举      — SELECT / INSERT / DELETE / CREATE INDEX
├── optimizer/             — SQL 优化器
│   ├── SQLOptimizer / ExecutionPlan
│   ├── FilterOperator / FilterOp / FilterValue
│   ├── VectorPushdownOperator  — 向量操作下推
│   ├── ProjectionOperator / LimitOperator
│   └── DistanceOp
└── SQLValue / SQLResult / SQLCondition
```

---

## 二、搜索与检索（6个模块）

### 7. coretex_ann — 近似最近邻

**文件**：`src/coretex_ann/mod.rs`

**职责**：ANN 算法参数配置与自动调优。

```
├── ANNConfig / ANNAlgorithm / ANNParameters
├── HNSWParameters / IVFParameters / PQParameters / NSGParameters
├── ANNTuner             — 自动参数调优器
├── IndexOptimizer       — 索引优化器
└── PerformanceRecord    — 性能记录
```

### 8. coretex_bm25 — 全文搜索

**文件**：`src/coretex_bm25.rs`

**职责**：BM25 关键词检索 + 混合搜索。

```
├── BM25Index             — BM25 索引（词频、文档频率、平均长度）
├── BM25Result            — 检索结果
├── HybridQueryEngine     — 混合查询引擎（向量 + 文本融合）
├── HybridSearchResult    — 混合结果
└── MetadataFilter        — 元数据过滤
```

### 9. coretex_embedding — 多模态嵌入

**文件**：`src/coretex_embedding/mod.rs`

**职责**：多模态数据的嵌入向量生成。

```
├── TextEmbeddingService        — 文本嵌入
├── ImageEmbeddingService       — 图像嵌入
├── AudioEmbeddingService       — 音频嵌入
├── VideoEmbeddingService       — 视频嵌入
├── PointCloudEmbeddingService  — 点云嵌入
├── EmbeddingRouter             — 自动路由到对应服务
├── StreamingEmbedder           — 流式嵌入
│   ├── BatchedStreamEmbedder
│   ├── WindowedStreamEmbedder
│   └── BackpressureStreamEmbedder（背压控制）
└── DataType 枚举               — Text/Image/Audio/Video/PointCloud
```

### 10. coretex_hybrid — 多模态混合检索

**文件**：`src/coretex_hybrid/mod.rs`

**职责**：跨多种数据类型的统一搜索。

```
├── MultiModalDocument   — 多模态文档（向量+文本+标量+时序+空间）
├── HybridQuery          — 混合查询
├── ScoreFusionEngine    — 分数融合引擎（RRF、加权和等）
├── HybridRetriever      — 混合检索器
│   ├── VectorRetriever
│   └── TextRetriever
└── FusedResult          — 融合后的排序结果
```

### 11. coretex_rerank — 两阶段重排序

**文件**：`src/coretex_rerank/mod.rs`

**职责**：粗排 → 精排两阶段搜索流水线。

```
├── CoarseRanker / CoarseRankerConfig   — 粗排（快速筛选）
├── FineRanker / FineRankerConfig       — 精排（精确打分）
├── TwoStageSearchPipeline              — 两阶段流水线
├── RerankDocument                      — 重排文档
└── RerankModel                         — 重排模型
```

### 12. coretex_search_pipeline — 搜索流水线

**文件**：`src/coretex_search_pipeline.rs`

**职责**：端到端搜索流水线编排。

```
功能：
├── 多路召回（向量 + BM25 + 标量过滤）
├── 分数融合
├── 重排序
└── 结果合并返回
```

---

## 三、分布式系统（5个模块）

### 13. coretex_distributed — 分布式事务 (2PC)

**文件**：`src/coretex_distributed/mod.rs`（722行）

**职责**：两阶段提交 (2PC) 协议的完整实现。

```
核心组件：
├── TwoPhaseCommit           — 2PC 协调者
│   ├── begin_transaction()  — 开启事务
│   ├── add_operation()      — 添加操作
│   ├── prepare()            — 阶段一：收集投票
│   ├── commit()             — 阶段二：提交
│   └── abort()              — 中止
├── DistributedTransaction   — 分布式事务
├── DistributedTransactionState 枚举
│   ├── Preparing → Prepared → Committing → Committed
│   └── Aborting → Aborted
├── ParticipantRpc (trait)   — 参与者 RPC 接口（可插拔）
├── LocalParticipantRpc      — 本地参与者实现
├── DistributedLockManager   — 分布式锁管理器
└── ParticipantState / ParticipantStatus
```

### 14. coretex_failover — 故障转移与 Raft

**文件**：`src/coretex_failover.rs`（31KB）

**职责**：集群高可用，基于 Raft 共识协议。

```
├── FailoverManager / FailoverConfig
├── RaftRpc (trait) / HttpRaftRpc
├── VoteRequest / VoteResponse           — 领导者选举
├── HeartbeatRequest / HeartbeatResponse — 心跳
├── AppendEntriesRequest/Response        — 日志复制
├── RaftLog / LogReplicator              — 日志管理
├── ConnectionPool                       — 连接池
├── NodeHealth / NodeStatus / ClusterStats
└── FailoverEvent
```

### 15. coretex_edge — 边缘计算

**文件**：`src/coretex_edge.rs`

**职责**：边缘节点部署与就近查询。

```
├── EdgeDB / EdgeConfig
├── EdgeStats / EdgeSearchResult
└── 边缘节点同步与本地缓存
```

### 16. coretex_grpc — gRPC 服务

**文件**：`src/coretex_grpc/mod.rs`

**职责**：gRPC 接口，支持拦截器链。

```
├── CoretexService         — gRPC 服务实现
├── GrpcConfig / GrpcMetrics
├── AuthInterceptor        — 认证拦截器
├── RateLimitInterceptor   — 限流拦截器
├── MetricsInterceptor     — 指标拦截器
├── ComposedInterceptor    — 组合拦截器
└── grpc_client            — 客户端
```

### 17. coretex_websocket — WebSocket 服务

**文件**：`src/coretex_websocket.rs`（31KB）

**职责**：实时双向通信。

```
├── WebSocketServer / WebSocketClient
├── WebSocketConfig
├── WebSocketMessage / WebSocketStats
├── HeartbeatManager       — 心跳管理
├── WsRateLimiter          — 限流
├── ConnectionState        — 连接状态
├── 认证消息：AuthRequest / AuthOkResponse
├── 搜索消息：SearchRequest / SearchResponse
├── 写入消息：InsertRequest / DeleteRequest
├── 订阅消息：SubscribeRequest / DataChangeEvent
└── 重连：ReconnectInfo
```

---

## 四、事务（3个模块）

### 18. coretex_transaction — 本地事务

**文件**：`src/coretex_transaction.rs`（43KB）

**职责**：ACID 本地事务，含 WAL 和锁管理。

```
├── TransactionManager      — 事务管理器
├── TransactionId / Snapshot
├── IsolationLevel 枚举     — 隔离级别
├── WriteAheadLog (WAL)     — 预写日志
│   ├── WalEntry / WalOperation
│   └── 崩溃恢复
├── LockManager             — 锁管理器
│   ├── LockMode（Shared/Exclusive）
│   ├── LockRequest
│   └── DeadlockInfo        — 死锁检测
└── TransactionError
```

### 19. coretex_cdc — 变更数据捕获

**文件**：`src/coretex_cdc.rs`（72KB）

**职责**：CDC 引擎，捕获并同步数据变更。

```
├── CdcEngine / CdcConfig / CdcEvent
├── CdcSource (trait)
│   ├── PostgresCdcSource
│   ├── MysqlCdcSource
│   └── MongodbCdcSource
├── VectorSyncHandler       — 向量同步处理器
├── VectorSyncEvent
└── SchemaChangeType
```

### 20. coretex_spatial_transaction — 空间事务（新增）

**文件**：`src/coretex_spatial_transaction/mod.rs`（519行）

**职责**：融合 RTree 空间索引 + 2PC 分布式事务 + TLS 安全传输。

```
三大支柱：
├── RTreeIndex                 — R-Tree 空间索引
│   ├── MBR（最小外接矩形）
│   ├── RTreeEntry / RTreeNode
│   ├── range_query()          — 范围查询
│   ├── knn_query()            — K 近邻查询
│   └── 三种分裂算法：
│       ├── Linear Split       — O(n)，选极端种子
│       ├── Quadratic Split    — O(n²)，最小面积增量
│       └── R*-tree Split      — O(n log n)，最优重叠+面积
├── SpatialTransaction         — 空间事务
│   ├── 状态：Preparing → Prepared → Committing → Committed
│   ├── SpatialOperation 枚举  — Insert / Delete / Update / Split
│   └── SpatialTxState
├── TlsSpatialCoordinator      — TLS 安全的 2PC 协调者
│   ├── TlsChannel             — TLS 加密通道
│   └── TlsHandshakeResult     — 握手结果（CN/密码套件/版本）
└── 场景：RTree 节点分裂跨分片时，2PC 保证原子性，
    所有协调者↔参与者通信经 TLS 1.3 加密
```

---

## 五、安全（3个模块）

### 21. coretex_security — 安全核心

**文件**：`src/coretex_security/mod.rs`（794行）

**职责**：TLS 传输加密 + 数据加密 + 审计 + ACL + KMS。

```
├── TLS 层
│   ├── TlsConfig / TlsServer / TlsClient
│   ├── 自签名证书生成（tls-gen feature）
│   ├── build_server_config() → rustls::ServerConfig
│   └── build_client_config() → rustls::ClientConfig
├── 加密层
│   ├── EncryptionService      — AES-256-GCM 加解密
│   ├── EncryptedData          — 加密数据格式（key_id + IV + 密文 + auth_tag）
│   ├── EncryptionKey          — 密钥（含过期时间）
│   └── KeyManager             — 密钥管理器
├── 密钥管理
│   ├── VaultKMS               — Vault KMS 集成
│   ├── KMSConfig / KMSProvider
│   ├── ExternalKey            — 外部密钥
│   └── KeyRotationManager     — 密钥轮换
├── 审计
│   ├── AuditLogger
│   ├── AuditEvent / AuditLevel / AuditAction
│   └── 审计日志持久化
├── 访问控制
│   ├── ACLEngine / ACLPolicy
│   ├── Subject / Resource / Action / Effect
│   └── ACLValidator
├── 网络安全
│   ├── NetworkIsolation / NetworkPolicy
│   ├── IpRange / IPRangeManager
│   └── PolicyAction（Allow/Deny）
└── 输入验证
    ├── InputValidator
    └── RateLimitValidator
```

### 22. coretex_auth — 认证与授权

**文件**：`src/coretex_auth/mod.rs`

**职责**：JWT 认证 + RBAC 权限模型。

```
├── AuthService
├── User / Role / Permission
├── JWTConfig / TokenClaims / AuthToken
├── UserInfo
└── RateLimiter
```

### 23. coretex_permissions — 权限管理

**文件**：`src/coretex_permissions.rs`

**职责**：细粒度权限控制。

---

## 六、专用索引（6个模块）

### 24. coretex_gis — 地理空间索引

**文件**：`src/coretex_gis/mod.rs`

**职责**：2D/3D 空间数据存储与查询。

```
├── GeoPoint / GeoBoundingBox / GeoPolygon / GeoLineString
├── GeoPoint3D / GeoPolygon3D / GeoBoundingBox3D
├── GeoIndex
└── GeoQuery（空间关系查询：包含/相交/邻近）
```

### 25. coretex_timeseries — 时序索引

**文件**：`src/coretex_timeseries/mod.rs`

**职责**：时序数据存储与聚合。

```
├── TimeSeriesIndex / TimeSeries
├── TimeSeriesPoint / TimeSeriesStats
├── Aggregation        — 聚合（Sum/Avg/Min/Max/Count）
├── RollingWindow      — 滑动窗口
└── ExponentialMovingAverage — 指数移动平均
```

### 26. coretex_graph — 图数据库

**文件**：`src/coretex_graph.rs`

**职责**：属性图存储与路径查询。

```
├── GraphDatabase
├── GraphNode / GraphEdge
├── GraphPath          — 路径结果
└── GraphError
```

### 27. coretex_document — 文档解析

**文件**：`src/coretex_document/mod.rs`

**职责**：非结构化文档解析（PDF/图像/音频）。

```
├── DocumentParser (trait)
├── DocumentParserRegistry
├── PdfParser / ImageParser / AudioParser
├── ParsedDocument / ImageData / TableData
├── HighDimVector / HighDimVectorStore
└── PQCompressor        — 乘积量化压缩
```

### 28. coretex_domain_index — 领域索引

**文件**：`src/coretex_domain_index.rs`

**职责**：面向特定领域的专用索引。

```
├── DomainIndex / DomainDocument / DomainSearchResult
├── DomainIndexManager
├── NewsWeatherIndex    — 新闻天气索引
├── GeoLocationIndex    — 地理位置索引
├── FinancialIndex      — 金融数据索引
└── KnowledgeIndex      — 知识库索引
```

### 29. coretex_lakehouse — 向量 Lakehouse

**文件**：`src/coretex_lakehouse/mod.rs`

**职责**：冷热数据分层存储。

```
├── StorageTier 枚举    — Hot / Warm / Cold
├── TierConfig
├── VectorLakehouse     — 数据湖仓
├── MigrationReport     — 迁移报告
├── LakehouseStats      — 统计
├── 分层策略：
│   ├── LRUTieringPolicy
│   ├── TTLTieringPolicy
│   └── HybridTieringPolicy
├── 存储后端：
│   ├── LocalConfig / S3Config
│   ├── MinIOConfig / AzureConfig
│   └── StorageBackend (trait)
└── DocumentMeta
```

---

## 七、运维（5个模块）

### 30. coretex_monitoring — 监控

**文件**：`src/coretex_monitoring/mod.rs`

**职责**：Prometheus 指标导出 + Grafana 集成。

```
├── PrometheusMetrics    — Prometheus 指标
├── DatabaseMetrics      — 数据库指标
├── AlertManager         — 告警管理
│   ├── AlertRule / AlertCondition / AlertSeverity
│   └── Alert
├── GrafanaConfig / GrafanaClient
└── SlowQueryLogger      — 慢查询日志
```

### 31. coretex_tracing — 分布式追踪

**文件**：`src/coretex_tracing.rs`

**职责**：请求跟踪与链路追踪。

### 32. coretex_backup — 备份恢复

**文件**：`src/coretex_backup.rs`（25KB）

**职责**：全量/增量备份与恢复。

### 33. coretex_persistence — 持久化

**文件**：`src/coretex_persistence.rs`

**职责**：数据持久化与快照。

### 34. coretex_observability_extra — 可观测性扩展

**文件**：`src/coretex_observability_extra.rs`（23KB）

**职责**：额外的可观测性功能。

---

## 八、优化与加速（4个模块）

### 35. coretex_compression — 向量压缩

**文件**：`src/coretex_compression/mod.rs`

**职责**：向量量化压缩以节省存储。

```
├── VectorCompressor / CompressedVector
├── CompressionAlgorithm 枚举
├── CompressionStats
├── RunLengthEncoding     — 游程编码
├── DeltaCoding           — 增量编码
└── QuantizationCompressor — 量化压缩
```

### 36. coretex_simd — SIMD 加速

**文件**：`src/coretex_simd.rs`

**职责**：CPU SIMD 指令加速向量计算。

```
├── simd_utils
├── SimdCapabilities     — 检测 CPU 支持的指令集
└── 自动 CPU 特性检测（cpufeatures）
```

### 37. coretex_incremental — 增量索引

**文件**：`src/coretex_incremental.rs`

**职责**：增量索引更新，避免全量重建。

```
├── IncrementalIndex
└── IndexUpdate
```

### 38. coretex_ha_extra — 高可用扩展

**文件**：`src/coretex_ha_extra.rs`（23KB）

**职责**：高可用性增强功能。

---

## 九、AI / ML（3个模块）

### 39. coretex_onnx — ONNX 推理

**文件**：`src/coretex_onnx.rs`

**职责**：ONNX 模型加载与推理（需 onnx feature）。

### 40. coretex_grpo — 强化学习优化

**文件**：`src/coretex_grpo.rs`（20KB）

**职责**：GRPO（Group Relative Policy Optimization）算法，用于索引参数自优化。

```
├── GRPOConfig           — 超参数配置
│   ├── learning_rate / clip_epsilon / kl_coefficient
│   ├── group_size / top_k_groups
│   ├── entropy_coefficient / max_grad_norm
│   └── discount_factor / gae_lambda
├── PolicyNetwork        — 策略网络（2层 MLP）
├── ValueNetwork         — 价值网络
├── GRPOTrainer          — 训练器
├── Trajectory           — 轨迹收集
└── GRPOOptimizer        — PPO-clip 优化
```

### 41. coretex_python — Python 绑定

**文件**：`src/coretex_python.rs`

**职责**：Python 接口（需 python feature）。

```
├── PyCortexDB / PyAsyncCortexDB
├── PySearchResult
├── PyCollectionInfo
├── PyHealth
└── PyCoreTexError
```

---

## 十、API 与 CLI（4个模块）

### 42. coretex_api — REST + GraphQL API

**文件**：`src/coretex_api/mod.rs`

**职责**：HTTP API 服务。

```
├── rest/
│   ├── start_server()      — Axum HTTP 服务器
│   └── ApiConfig
└── graphql/
    ├── AppSchema / build_schema()
    ├── QueryRoot / MutationRoot / SubscriptionRoot
    ├── start_graphql_server()
    └── Gql 类型（CollectionSchema, SearchResultItem 等）
```

### 43. coretex_cli — 命令行界面

**文件**：`src/coretex_cli/mod.rs`

**职责**：CLI 工具（基于 clap）。

### 44. coretex_export — 数据导出

**文件**：`src/coretex_export/mod.rs`

**职责**：数据导出为多种格式。

```
├── DataExporter / VectorExporter
├── BatchExporter / CollectionExporter
├── ExportResult
└── ExportFormat（JSON/CSV/Parquet/Binary）
```

### 45. coretex_utils — 通用工具

**文件**：`src/coretex_utils/mod.rs`

**职责**：集群管理 + LRU 缓存 + 向量工具函数。

```
├── ClusterManager / ClusterNode / NodeRole / NodeState / Shard
├── LRUCache / TimedLRUCache / AsyncLRUCache
├── MultiLevelCache / CacheStats / MultiLevelCacheStats
├── 向量函数：
│   ├── cosine_similarity()
│   ├── euclidean_distance()
│   ├── normalize_vector()
│   ├── parse_vector()
│   └── random_vector()
```

---

## 十一、领域扩展（2个模块）（v1.2 新增）

### 46. coretex_bio — 生物信息学

**文件**：`src/coretex_bio.rs`（613行）

**职责**：DNA/蛋白质序列的 k-mer 索引与向量化。

```
├── KmerIndexer              — k-mer 提取器（支持 DNA/蛋白质字母表）
├── SequenceChunker          — 序列分块器
├── SequenceChunk / SequenceChunkWithMeta
├── BinaryVector / IntegerVector
├── SpacetimeIndex           — 时空索引
├── SpacetimePoint           — 时空点
├── UserDefinedFunction      — UDF 支持
├── UdfType / UdfParamType
└── UdfRegistry
```

### 47. coretex_types_extra — 数据类型扩展

**文件**：`src/coretex_types_extra.rs`（1050行）

**职责**：增强的数据类型系统——3D 拓扑、时序窗口函数、图算法、RAG 跨模态、领域索引。

```
├── 3D GIS 拓扑
│   ├── DE9IM                  — 9 交叉矩阵
│   ├── SpatialRelation        — 空间关系（8 种）
│   ├── GeoPoint3D / GeoBoundingBox3D
│   └── Topology3D
├── 时序窗口函数
│   ├── WindowType / WindowFunction
│   ├── WindowResult / TimeSeriesWindow
│   └── ExponentialMovingAverage 等
├── 图算法
│   ├── GNode / GEdge / Graph
│   ├── PageRank
│   ├── ConnectedComponents
│   └── Dijkstra 最短路径
├── RAG 跨模态
│   ├── DocumentChunk
│   ├── RagResult / RagRetriever
│   └── 跨模态文档链路
├── 电商索引
│   ├── Product / Order / InventoryItem
│   └── ECommerceIndex
├── 医疗索引
│   ├── Patient / Diagnosis / Drug
│   └── MedicalIndex
└── 物流索引
    ├── Package / Route / Carrier
    └── LogisticsIndex
```

---

## 模块依赖关系图

```
                    ┌──────────────┐
                    │  coretex_api │  ← REST + GraphQL
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
      ┌───────────┐ ┌───────────┐ ┌───────────┐
      │coretex_cli│ │coretex_   │ │coretex_   │
      │           │ │websocket  │ │grpc       │
      └─────┬─────┘ └─────┬─────┘ └─────┬─────┘
            │             │             │
            └─────────┬───┴─────────────┘
                      ▼
              ┌───────────────┐
              │  coretex_data │  ← 统一数据层
              └───────┬───────┘
                      │
      ┌───────────────┼───────────────────┐
      ▼               ▼                   ▼
┌───────────┐  ┌─────────────┐   ┌───────────────┐
│coretex_   │  │ coretex_    │   │ coretex_      │
│storage    │  │ index       │   │ transaction   │
└───────────┘  └──────┬──────┘   └───────┬───────┘
                      │                  │
      ┌───────────────┼──────────┐       │
      ▼               ▼          ▼       ▼
┌───────────┐  ┌───────────┐ ┌──────────────┐
│coretex_ann│  │coretex_   │ │coretex_       │
│           │  │bm25       │ │spatial_       │
│ HNSW/IVF  │  │全文检索    │ │transaction   │
└───────────┘  └───────────┘ │ RTree+2PC+TLS │
                             └───────────────┘

安全层：coretex_security → coretex_auth → coretex_permissions
分布式：coretex_distributed → coretex_failover → coretex_edge
搜索链：embedding → hybrid → rerank → search_pipeline
运维链：monitoring → tracing → observability → backup
```

---

## 快速导航

| 需求场景 | 对应模块 |
|---------|---------|
| 向量相似性搜索 | `coretex_index` → `coretex_ann` |
| 全文关键词搜索 | `coretex_bm25` |
| 混合搜索（向量+文本） | `coretex_hybrid` → `coretex_rerank` |
| 空间范围查询 / KNN | `coretex_gis` → `coretex_spatial_transaction` |
| 分布式事务 (2PC) | `coretex_distributed` → `coretex_transaction` |
| CDC 数据同步 | `coretex_cdc` |
| SQL 查询 | `coretex_sql` |
| TLS 加密通信 | `coretex_security` |
| 集群高可用 / Raft | `coretex_failover` |
| 冷热数据分层 | `coretex_lakehouse` |
| Prometheus 监控 | `coretex_monitoring` |
| 向量压缩节省存储 | `coretex_compression` |
| ONNX 模型推理 | `coretex_onnx` |
| Python 绑定 | `coretex_python` |
| 图数据库 | `coretex_graph` |
| 时序数据分析 | `coretex_timeseries` |
| 文档解析 (PDF/图像) | `coretex_document` |
| 生物信息学 k-mer | `coretex_bio` |
| 3D 拓扑 / 图算法 / 电商医疗物流 | `coretex_types_extra` |

---

> **总计**：47 个功能模块，全部接入编译，覆盖向量检索、全文搜索、空间索引、分布式事务、安全加密、监控运维、AI 推理、生物信息学、领域扩展等全部能力。
