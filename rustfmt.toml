# Rustfmt settings — run `cargo fmt` before opening a PR.
# See CONTRIBUTING.md §4 Code style.
edition = "2021"
max_width = 100
use_field_init_shorthand = true
use_small_heuristics = "Default"
reorder_imports = true
reorder_modules = true
newline_style = "Unix"
